# config/logging/

`logback.xml`: structured logging configuration with correlation IDs. See
`FINAL_ARCHITECTURE.md`, "Observability is a checklist, not an operational
design." Not yet committed — Step 1 scope is directory skeleton only.
