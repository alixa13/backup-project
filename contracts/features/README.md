# contracts/features/

Owns `conn-feature-schema-v1.json`: the 20 ordered float32 `conn-feature-v1`
values, their formulas/units/defaults, category mappings, and window/key/watermark
semantics, plus the schema's content hash. AI owns semantics, software owns
execution. Frozen Day 2, window semantics Day 5 (Roadmap.md Section 5). Not yet
committed — Step 1 scope is directory skeleton only.
