/**
 * Bounded keyed state: fixed rolling numeric buckets per (sensor, sourceIp). No unbounded per-IP maps or event history.
 *
 * <p>Step 1 scope: package skeleton only, no implementation yet.
 */
package io.netsecml.platform.adapter.flink.state;
