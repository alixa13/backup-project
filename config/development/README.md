# config/development/

Non-secret development configuration: `online-job.yaml`, `archive-job.yaml`,
`resources.yaml`. No credentials — see `.env.example` at the repo root for the
variable names that supply secrets at runtime. Not yet committed — Step 1 scope is
directory skeleton only.
