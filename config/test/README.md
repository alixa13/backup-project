# config/test/

Non-secret configuration for integration/E2E test profiles. Mirrors
`config/development/` with test-scoped topic names, retention, and resource caps.
Not yet committed — Step 1 scope is directory skeleton only.
