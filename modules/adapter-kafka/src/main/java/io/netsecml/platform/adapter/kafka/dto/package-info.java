/**
 * Kafka JSON DTOs (e.g. ZeekConnEvent). DTOs are adapter-local and must never escape into the domain module.
 *
 * <p>Step 1 scope: package skeleton only, no implementation yet.
 */
package io.netsecml.platform.adapter.kafka.dto;
