# contracts/source/

Owns `zeek-conn-source-v1.json`: the compatibility contract for the external Kafka
`conn` topic — required field/types, `DateTime` epoch-seconds convention, ID/sensor
policy, additive-field policy, and invalid reason codes. Frozen Day 1 (Roadmap.md
Section 5). Not yet committed — Step 1 scope is directory skeleton only.
