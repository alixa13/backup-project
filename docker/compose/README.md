# docker/compose/

`docker-compose.dev.override.yml`: extends the team's existing local Kafka/Flink/
ClickHouse services for this platform's needs. Does not duplicate or replace those
services. Not yet committed — Step 1 scope is directory skeleton only.
