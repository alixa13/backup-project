{
  "id": "modbus-feature-v1",
  "semanticVersion": "1.0.0",
  "inputDtype": "float32",
  "featureCount": 42,
  "notes": "Mirrors two-models-info/modbus_/FEATURE_CONTRACT_V1.json (committed here as tests/fixtures/contracts/modbus_feature_contract_v1.json) feature-for-feature: same 42 names in the same order, this schema's index 0-based against the upstream contract's 1-based index. Every missingPolicy is DEFAULT_ZERO -- the upstream contract's missing_rule values (\"required\", \"never missing\", \"0 if absent\", \"use address_present\") all describe a value that is present or defaulted to zero, never one this schema rejects as missing, so DEFAULT_ZERO throughout is the honest mapping; REQUIRED would claim a rejection path that does not exist here. This schema carries no common tier: it mirrors an externally frozen contract, so it carries exactly what that contract specifies (docs/superpowers/specs/2026-09-21-modbus-stage1-design.md section 9, DECIDED 1).",
  "features": [
    { "index": 0, "name": "is_response", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1=response; 0=request" },
    { "index": 1, "name": "fc_1", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff FC=1" },
    { "index": 2, "name": "fc_2", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff FC=2" },
    { "index": 3, "name": "fc_3", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff FC=3" },
    { "index": 4, "name": "fc_4", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff FC=4" },
    { "index": 5, "name": "fc_5", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff FC=5" },
    { "index": 6, "name": "fc_6", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff FC=6" },
    { "index": 7, "name": "fc_other", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff FC not in {1..6}" },
    { "index": 8, "name": "address_value", "unit": "address", "missingPolicy": "DEFAULT_ZERO", "formula": "numeric address; 0 if absent" },
    { "index": 9, "name": "address_present", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff address present" },
    { "index": 10, "name": "quantity_value", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "numeric quantity; 0 if absent" },
    { "index": 11, "name": "quantity_present", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff quantity present" },
    { "index": 12, "name": "response_matched", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "response current causal matched flag; request=0" },
    { "index": 13, "name": "request_values_present", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff numeric request values present" },
    { "index": 14, "name": "request_value_count", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "count numeric request values" },
    { "index": 15, "name": "request_value_min", "unit": "value", "missingPolicy": "DEFAULT_ZERO", "formula": "min numeric request value" },
    { "index": 16, "name": "request_value_max", "unit": "value", "missingPolicy": "DEFAULT_ZERO", "formula": "max numeric request value" },
    { "index": 17, "name": "request_value_mean", "unit": "value", "missingPolicy": "DEFAULT_ZERO", "formula": "mean numeric request value" },
    { "index": 18, "name": "response_values_present", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff numeric response values present" },
    { "index": 19, "name": "response_value_count", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "count numeric response values" },
    { "index": 20, "name": "response_value_min", "unit": "value", "missingPolicy": "DEFAULT_ZERO", "formula": "min numeric response value" },
    { "index": 21, "name": "response_value_max", "unit": "value", "missingPolicy": "DEFAULT_ZERO", "formula": "max numeric response value" },
    { "index": 22, "name": "response_value_mean", "unit": "value", "missingPolicy": "DEFAULT_ZERO", "formula": "mean numeric response value" },
    { "index": 23, "name": "prev_event_available", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff prior event exists in current C3 segment" },
    { "index": 24, "name": "inter_arrival_s", "unit": "seconds", "missingPolicy": "DEFAULT_ZERO", "formula": "current ts - previous ts in segment" },
    { "index": 25, "name": "function_changed", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff FC differs from previous event" },
    { "index": 26, "name": "address_delta_valid", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff current and previous applicable address exist" },
    { "index": 27, "name": "address_delta", "unit": "address", "missingPolicy": "DEFAULT_ZERO", "formula": "current address - previous applicable address" },
    { "index": 28, "name": "quantity_delta_valid", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff current and previous applicable quantity exist" },
    { "index": 29, "name": "quantity_delta", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "current quantity - previous applicable quantity" },
    { "index": 30, "name": "outstanding_requests_before_event", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "pending TID count before current event" },
    { "index": 31, "name": "response_without_request", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "response with no pending matching TID" },
    { "index": 32, "name": "request_overwrite_same_tid", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "request reuses already-pending TID" },
    { "index": 33, "name": "rtt_valid", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 iff response matched prior request causally" },
    { "index": 34, "name": "rtt_s", "unit": "seconds", "missingPolicy": "DEFAULT_ZERO", "formula": "response ts - matched request ts" },
    { "index": 35, "name": "event_rate_1s", "unit": "count/s", "missingPolicy": "DEFAULT_ZERO", "formula": "count in (t-1,t] / 1s" },
    { "index": 36, "name": "event_rate_10s", "unit": "count/s", "missingPolicy": "DEFAULT_ZERO", "formula": "count in (t-10,t] / 10s" },
    { "index": 37, "name": "event_rate_60s", "unit": "count/s", "missingPolicy": "DEFAULT_ZERO", "formula": "count in (t-60,t] / 60s" },
    { "index": 38, "name": "unique_function_count_10s", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "distinct FCs in (t-10,t]" },
    { "index": 39, "name": "unique_address_count_10s", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "distinct present addresses in (t-10,t]" },
    { "index": 40, "name": "read_ratio_10s", "unit": "ratio", "missingPolicy": "DEFAULT_ZERO", "formula": "READ/READ_WRITE fraction in (t-10,t]" },
    { "index": 41, "name": "write_ratio_10s", "unit": "ratio", "missingPolicy": "DEFAULT_ZERO", "formula": "WRITE/READ_WRITE fraction in (t-10,t]" }
  ]
}
