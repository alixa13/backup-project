/**
 * In-process ONNX Runtime session management and inference execution (CPU only, one session per subtask).
 *
 * <p>Step 1 scope: package skeleton only, no implementation yet.
 */
package io.netsecml.platform.adapter.onnx.runtime;
