# config/production/

Non-secret production configuration. No credentials ever committed here — secrets
are supplied at deploy time per `.env.example`. Populated starting Roadmap.md Day 11
(deployment config, Roadmap.md Section 5). Not yet committed.
