/**
 * Typed, minimal connection facts (e.g. NetworkEvent, ConnectionTuple, ConnectionMeasurements). Pure values, no infrastructure imports.
 *
 * <p>Step 1 scope: package skeleton only, no implementation yet.
 */
package io.netsecml.platform.domain.event;
