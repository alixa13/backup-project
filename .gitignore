# Python
__pycache__/
*.py[cod]
*.so

# Environments
.venv/
venv/
*/.venv/
*/venv/

# IDE
.vscode/
*.swp

# Logs & temp
*.log
monitor_log_*.txt

# Data/model artifacts
lstm-autoencoder/data/
lstm-autoencoder/app/data/
lstm-autoencoder/models/
supervised/tmp/
supervised/API/app/models/*.joblib
supervised/API/app/models/*.h5
supervised/API/app/models/*.pb
supervised/API/app/models/*.ckpt
supervised/API/app/models/*.pkl
supervised/API/app/models/*.sav
supervised/API/app/models/*.pt

# Build outputs
flink/jars/
FlinkKafkaProject/target/

# Captures and large samples
pcap/

# Misc
.DS_Store

# ── Added: things that were one `git add -A` away from being committed ──────────
# backups/ is 1.6GB and contains a full Python venv (backups/supervised/supervised/ai).
# It is a second copy of the project, not a backup strategy - move it outside the repo.
backups/

# Kafka topic dumps (210MB) and training datasets (46MB)
topic-exports/
supervised/data/
NUSW-NB15_features.csv

# Trained model artifacts and training exhaust
supervised/models/
supervised/catboost_info/
*.pkl
*.joblib

# Runtime state
*.pid
training-signals/
.clear-data-on-next-start

# Vendored jars: build them with `mvn package` instead
flink/*.jar
backups/supervised/supervised/ai/
topic-exports/
