# contracts/model/

Owns `model-bundle-manifest-v1.json`: exact model version/SHA, schema ID/hash/count,
ONNX input/output names and shapes, class index, decision threshold, and
snapshot/code/dependency hashes. Both engineers own this contract. Frozen Days 7-8
(Roadmap.md Section 5). Not yet committed — Step 1 scope is directory skeleton only.
