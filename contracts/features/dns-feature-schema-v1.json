{
  "id": "dns-feature-v1",
  "semanticVersion": "1.0.0",
  "inputDtype": "float32",
  "featureCount": 24,
  "features": [
    { "index": 0, "name": "record_count_5m", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "records for (sensor, sourceIp) key in trailing 5-minute rolling window" },
    { "index": 1, "name": "byte_sum_5m", "unit": "bytes", "missingPolicy": "DEFAULT_ZERO", "formula": "byte total for (sensor, sourceIp) key in trailing 5-minute rolling window" },
    { "index": 2, "name": "failed_count_5m", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "failed records for (sensor, sourceIp) key in trailing 5-minute rolling window" },
    { "index": 3, "name": "inter_arrival_mean_ms", "unit": "milliseconds", "missingPolicy": "DEFAULT_ZERO", "formula": "running mean interval between records for (sensor, sourceIp) key" },
    { "index": 4, "name": "inter_arrival_stddev_ms", "unit": "milliseconds", "missingPolicy": "DEFAULT_ZERO", "formula": "running population stddev of the inter-arrival interval for (sensor, sourceIp) key" },
    { "index": 5, "name": "is_orig", "unit": "boolean", "missingPolicy": "REQUIRED", "formula": "1 if the record was sent by the connection originator else 0" },
    { "index": 6, "name": "conn_orig_bytes", "unit": "bytes", "missingPolicy": "DEFAULT_ZERO", "formula": "orig_bytes delta since the previous conn.log snapshot" },
    { "index": 7, "name": "conn_resp_bytes", "unit": "bytes", "missingPolicy": "DEFAULT_ZERO", "formula": "resp_bytes delta since the previous conn.log snapshot" },
    { "index": 8, "name": "conn_orig_pkts", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "orig_pkts delta since the previous conn.log snapshot" },
    { "index": 9, "name": "conn_resp_pkts", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "resp_pkts delta since the previous conn.log snapshot" },
    { "index": 10, "name": "conn_age_seconds", "unit": "seconds", "missingPolicy": "DEFAULT_ZERO", "formula": "connection age at the time of the latest conn.log snapshot" },
    { "index": 11, "name": "conn_enrichment_present", "unit": "boolean", "missingPolicy": "REQUIRED", "formula": "1 if a conn.log snapshot was available for this record else 0" },
    { "index": 12, "name": "dns_rcode", "unit": "code", "missingPolicy": "DEFAULT_ZERO", "formula": "IANA RCODE number; 0 (NOERROR) when no response" },
    { "index": 13, "name": "dns_qtype", "unit": "code", "missingPolicy": "REQUIRED", "formula": "IANA QTYPE number from qtype" },
    { "index": 14, "name": "dns_authoritative", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 if AA else 0" },
    { "index": 15, "name": "dns_recursion_available", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 if RA else 0" },
    { "index": 16, "name": "dns_truncated", "unit": "boolean", "missingPolicy": "DEFAULT_ZERO", "formula": "1 if TC else 0" },
    { "index": 17, "name": "dns_answer_count", "unit": "count", "missingPolicy": "DEFAULT_ZERO", "formula": "length of answers" },
    { "index": 18, "name": "dns_ttl", "unit": "seconds", "missingPolicy": "DEFAULT_ZERO", "formula": "first element of TTLs" },
    { "index": 19, "name": "dns_qname_length", "unit": "count", "missingPolicy": "REQUIRED", "formula": "character count of query" },
    { "index": 20, "name": "dns_qname_entropy", "unit": "bits", "missingPolicy": "REQUIRED", "formula": "Shannon entropy over query characters" },
    { "index": 21, "name": "dns_label_count", "unit": "count", "missingPolicy": "REQUIRED", "formula": "dot-separated label count of query" },
    { "index": 22, "name": "dns_digit_ratio", "unit": "ratio", "missingPolicy": "REQUIRED", "formula": "digits / length of query" },
    { "index": 23, "name": "dns_hyphen_ratio", "unit": "ratio", "missingPolicy": "REQUIRED", "formula": "hyphens / length of query" }
  ]
}
